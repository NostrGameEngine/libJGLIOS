diff --git a/src/libANGLE/renderer/metal/DisplayMtl.mm b/src/libANGLE/renderer/metal/DisplayMtl.mm
index cdfd06b3f7..9275760760 100644
--- a/src/libANGLE/renderer/metal/DisplayMtl.mm
+++ b/src/libANGLE/renderer/metal/DisplayMtl.mm
@@ -1013,7 +1013,8 @@ void DisplayMtl::initializeExtensions() const
 
     mNativeExtensions.sampleVariablesOES = true;
 
-    if ([mMetalDevice supportsPullModelInterpolation])
+    if ([mMetalDevice respondsToSelector:@selector(supportsPullModelInterpolation)] &&
+        [mMetalDevice supportsPullModelInterpolation])
     {
         mNativeExtensions.shaderMultisampleInterpolationOES = true;
         mNativeCaps.subPixelInterpolationOffsetBits         = 4;
@@ -1365,7 +1366,11 @@ bool DisplayMtl::supportsEitherGPUFamily(uint8_t iOSFamily, uint8_t macFamily) c
 bool DisplayMtl::supports32BitFloatFiltering() const
 {
 #if !TARGET_OS_WATCH
-    return [mMetalDevice supports32BitFloatFiltering];
+    if ([mMetalDevice respondsToSelector:@selector(supports32BitFloatFiltering)])
+    {
+        return [mMetalDevice supports32BitFloatFiltering];
+    }
+    return false;
 #else
     return false;
 #endif
diff --git a/src/libANGLE/renderer/metal/mtl_context_device.mm b/src/libANGLE/renderer/metal/mtl_context_device.mm
index 6aa02c0780..ac42dc9809 100644
--- a/src/libANGLE/renderer/metal/mtl_context_device.mm
+++ b/src/libANGLE/renderer/metal/mtl_context_device.mm
@@ -134,7 +134,16 @@ void ContextDevice::setOwnerWithIdentity(id<MTLResource> resource) const
 
 bool ContextDevice::hasUnifiedMemory() const
 {
-    return [get() hasUnifiedMemory];
+    if ([get() respondsToSelector:@selector(hasUnifiedMemory)])
+    {
+        return [get() hasUnifiedMemory];
+    }
+    // hasUnifiedMemory requires iOS 14+. On older iOS all A-series chips have unified memory.
+#if TARGET_OS_IOS && !TARGET_OS_MACCATALYST
+    return true;
+#else
+    return false;
+#endif
 }
 
 }  // namespace mtl
diff --git a/src/libANGLE/renderer/metal/mtl_utils.mm b/src/libANGLE/renderer/metal/mtl_utils.mm
index acb165ef23..ffe2af0cb3 100644
--- a/src/libANGLE/renderer/metal/mtl_utils.mm
+++ b/src/libANGLE/renderer/metal/mtl_utils.mm
@@ -1373,34 +1373,41 @@ bool DeviceHasMaximumRenderTargetSize(id<MTLDevice> device)
 
 bool SupportsAppleGPUFamily(id<MTLDevice> device, uint8_t appleFamily)
 {
-    MTLGPUFamily family;
+    // supportsFamily: requires iOS 13+. Use respondsToSelector: to guard it and fall back
+    // to the deprecated supportsFeatureSet: API (available since iOS 8) on older systems.
+    if ([device respondsToSelector:@selector(supportsFamily:)])
+    {
+        MTLGPUFamily family;
+        switch (appleFamily)
+        {
+            case 1: family = MTLGPUFamilyApple1; break;
+            case 2: family = MTLGPUFamilyApple2; break;
+            case 3: family = MTLGPUFamilyApple3; break;
+            case 4: family = MTLGPUFamilyApple4; break;
+            case 5: family = MTLGPUFamilyApple5; break;
+            case 6: family = MTLGPUFamilyApple6; break;
+            case 7: family = MTLGPUFamilyApple7; break;
+            default: return false;
+        }
+        return [device supportsFamily:family];
+    }
+#if TARGET_OS_IOS && !TARGET_OS_MACCATALYST
+    // Fallback for iOS < 13: MTLFeatureSet is deprecated in iOS 16 but available since iOS 8.
+#    pragma clang diagnostic push
+#    pragma clang diagnostic ignored "-Wdeprecated-declarations"
     switch (appleFamily)
     {
-        case 1:
-            family = MTLGPUFamilyApple1;
-            break;
-        case 2:
-            family = MTLGPUFamilyApple2;
-            break;
-        case 3:
-            family = MTLGPUFamilyApple3;
-            break;
-        case 4:
-            family = MTLGPUFamilyApple4;
-            break;
-        case 5:
-            family = MTLGPUFamilyApple5;
-            break;
-        case 6:
-            family = MTLGPUFamilyApple6;
-            break;
-        case 7:
-            family = MTLGPUFamilyApple7;
-            break;
-        default:
-            return false;
+        case 1: return [device supportsFeatureSet:MTLFeatureSet_iOS_GPUFamily1_v1];
+        case 2: return [device supportsFeatureSet:MTLFeatureSet_iOS_GPUFamily2_v1];
+        case 3: return [device supportsFeatureSet:MTLFeatureSet_iOS_GPUFamily3_v1];
+        case 4: return [device supportsFeatureSet:MTLFeatureSet_iOS_GPUFamily4_v1];
+        case 5: return [device supportsFeatureSet:MTLFeatureSet_iOS_GPUFamily5_v1];
+        default: return false;  // Family 6+ (A14+) does not exist on iOS < 13
     }
-    return [device supportsFamily:family];
+#    pragma clang diagnostic pop
+#else
+    return false;
+#endif
 }
 
 bool SupportsMacGPUFamily(id<MTLDevice> device, uint8_t macFamily)
